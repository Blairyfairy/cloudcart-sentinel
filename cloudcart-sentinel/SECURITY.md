# Security policy

Please do not open public issues for suspected vulnerabilities. Report them privately through GitHub Security Advisories.

Supported version: `1.x`.

The local credentials in `.env.example` are development-only. Production deployments must use a managed identity provider, encrypted secret storage, TLS, private database/cache networking, restricted egress, image scanning, and dependency update automation.
